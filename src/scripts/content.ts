// ドキュメント内に「ぱすはに」という文字列があったらその文字に「❤️」を追加する
const targetText = 'ぱすはに';
const replaceText = '❤️';

const targetElements = document.querySelectorAll('*');
targetElements.forEach((element) => {
  if (element.textContent) {
    // タグ
  }
});
